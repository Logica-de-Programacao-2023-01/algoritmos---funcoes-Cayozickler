func calcularMedia(slice []int) float64 {
    if len(slice) == 0 {
        return 0.0
    }

    soma := 0
    for _, valor := range slice {
        soma += valor
    }

    return float64(soma) / float64(len(slice))
}
